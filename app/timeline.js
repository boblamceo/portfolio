import React from "react";

const Timeline = ({ innerref }) => {
    return (
        <div
            ref={innerref}
            className="w-screen bg-black min-h-screen text-white"
        >
            hi
        </div>
    );
};

export default Timeline;
